[project]
name = "mn-doc-poc"
dynamic = ["version"]
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.119.0",
    "pydantic-settings>=2.11.0",
    "uvicorn>=0.37.0",
    "databricks-sdk>=0.74.0",
]

[dependency-groups]
dev = [
    "ty>=0.0.12",
]

[tool.apx.metadata]
app-name = "mn-doc-poc"
app-slug = "mn_doc_poc"
app-entrypoint = "mn_doc_poc.backend.app:app"
api-prefix = "/api"
metadata-path = "src/mn_doc_poc/_metadata.py"

[tool.apx.ui]
root = "src/mn_doc_poc/ui"

[tool.apx.ui.registries]
"@animate-ui" = "https://animate-ui.com/r/{name}.json"
"@ai-elements" = "https://registry.ai-sdk.dev/{name}.json"
"@svgl" = "https://svgl.app/r/{name}.json"

[build-system]
requires = ["hatchling", "uv-dynamic-versioning>=0.7.0"]
build-backend = "hatchling.build"

[tool.hatch.version]
source = "uv-dynamic-versioning"

[tool.uv-dynamic-versioning]
vcs = "git"
fallback-version = "0.0.0"

[tool.hatch.build.hooks.version]
path = "src/mn_doc_poc/_version.py"
template = '''
version = "{version}"
'''

[tool.hatch.build]
artifacts = ["src/mn_doc_poc/__dist__", "src/mn_doc_poc/_metadata.py"]
exclude = ["src/mn_doc_poc/ui"]
